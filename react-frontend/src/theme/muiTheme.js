import { createMuiTheme } from '@material-ui/core/styles';

const palette = {
  primary: { main: '#c10000' },
  secondary: { main: '#ffd13a' }
};
const themeName = 'Guardsman Red Bright Sun Grizzly Bear';

export default createMuiTheme({ palette, themeName });
